#!/bin/sh
for file in $(find . -maxdepth 1 -type f); do
    if [[ "$file" =~ [0-9] ]]; then
        echo "$file contains a number."
    else
        echo "$file does not contain a number."
    fi
done

